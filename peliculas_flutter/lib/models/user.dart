class User {
  static const roleName = 'user';

  String id, username, uid, name, surname, email;

  User(
    this.id,
    this.username,
    this.uid,
    this.name,
    this.surname,
    this.email,
  );
}
